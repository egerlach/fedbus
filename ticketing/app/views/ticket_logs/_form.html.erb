<%= form_for(@ticket_log) do |f| %>
  <% if @ticket_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_log.errors.count, "error") %> prohibited this ticket_log from being saved:</h2>

      <ul>
      <% @ticket_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :log %><br />
    <%= f.text_field :log %>
  </div>
  <div class="field">
    <%= f.label :ticket %><br />
    <%= f.text_field :ticket %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
